context:
  name: pkn
  version: "0.5.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.tar.gz
  sha256: 52f411a68907bad82ef96bca3fb83f21c2ac39dc6b53d37c5b7c052ed2e969f8

build:
  number: 0
  noarch: python
  script: python -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - hatchling
    - hatch-multi
  run:
    - python >=${{ python_min }}
    - boto3
    - botocore
    - ccflow >=0.6.10
    - ccflow-s3
    - ccflow-email
    - colorlog
    - emails
    - jinja2
    - pydantic

tests:
  - python:
      imports:
        - pkn
        - pkn.ccflow
        - pkn.infra
        - pkn.logging
        - pkn.pydantic
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  license: Apache-2.0
  license_file: LICENSE
  summary: My Pocket Knife - miscellaneous utilities
  homepage: https://github.com/timkpaine/pkn
  repository: https://github.com/timkpaine/pkn

extra:
  recipe-maintainers:
    - timkpaine
